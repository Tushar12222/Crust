{
    "name": "{{crate_name}}",
    "version": "0.1.0",
    "description": "",
    "scripts": {
      "setup": "rustup target add wasm32-unknown-unknown && cargo install trunk && npm i -g tailwindcss",
      "dev": "trunk serve --open",
      "prod": "wasm-pack build --target web"
    }
}